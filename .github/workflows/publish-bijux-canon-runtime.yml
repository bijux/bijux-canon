name: publish-bijux-canon-runtime

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build-release-artifacts.yml
    with:
      package_slug: bijux-canon-runtime
      package_dir: packages/bijux-canon-runtime
      artifacts_dir: artifacts/bijux-canon-runtime
      cache_dependency_path: |
        packages/bijux-canon-runtime/pyproject.toml
        pyproject.toml
      build_targets: "build sbom"
      upload_paths: |
        artifacts/bijux-canon-runtime/build/*
        artifacts/bijux-canon-runtime/sbom/*

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      contents: read
    steps:
      - name: Download publish artifacts
        uses: actions/download-artifact@v5
        with:
          name: bijux-canon-runtime-pypi-dist
          path: .github/tmp/bijux-canon-runtime-dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          packages-dir: .github/tmp/bijux-canon-runtime-dist
          skip-existing: true
