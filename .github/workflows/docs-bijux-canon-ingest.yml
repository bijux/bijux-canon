name: docs-bijux-canon-ingest

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/docs-bijux-canon-ingest.yml"
      - "packages/bijux-canon-ingest/docs/**"
      - "packages/bijux-canon-ingest/CHANGELOG.md"
      - "apis/bijux-canon-ingest/**"
      - "scripts/bijux-canon-ingest/**"
      - "packages/bijux-canon-ingest/mkdocs.yml"
      - "makes/docs.mk"
      - "makes/shared/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: docs-bijux-canon-ingest-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: packages/bijux-canon-ingest/pyproject.toml

      - name: Install package docs toolchain
        run: make -C packages/bijux-canon-ingest install

      - name: Configure Git author
        run: |
          git config user.name "Bijan Mousavi"
          git config user.email "bijan@bijux.io"

      - name: Deploy docs
        run: make -C packages/bijux-canon-ingest docs-deploy
