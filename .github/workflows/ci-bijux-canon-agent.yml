name: ci-bijux-canon-agent

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ci-bijux-canon-agent.yml"
      - ".github/workflows/ci-package.yml"
      - "packages/bijux-canon-agent/**"
      - "apis/bijux-canon-agent/**"
      - "configs/**"
      - "makes/**"
      - "Makefile"
      - "pyproject.toml"
      - "tox.ini"
      - "packages/bijux-canon-dev/**"
  pull_request:
    paths:
      - ".github/workflows/ci-bijux-canon-agent.yml"
      - ".github/workflows/ci-package.yml"
      - "packages/bijux-canon-agent/**"
      - "apis/bijux-canon-agent/**"
      - "configs/**"
      - "makes/**"
      - "Makefile"
      - "pyproject.toml"
      - "tox.ini"
      - "packages/bijux-canon-dev/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci-package.yml
    with:
      package_slug: bijux-canon-agent
      package_dir: packages/bijux-canon-agent
      artifacts_dir: artifacts/bijux-canon-agent
      cache_dependency_path: |
        packages/bijux-canon-agent/pyproject.toml
        pyproject.toml
        tox.ini
