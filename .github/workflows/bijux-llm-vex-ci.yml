name: bijux-llm-vex CI

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/bijux-llm-vex-ci.yml"
      - "packages/bijux-llm-vex/**"
      - "apis/bijux-llm-vex/**"
      - "configs/bijux-vex/**"
      - "makes/bijux-vex/**"
      - "makes/lint.mk"
      - "makes/test.mk"
      - "makes/api.mk"
      - "makes/quality.mk"
      - "makes/security.mk"
      - "makes/sbom.mk"
      - "makes/publish.mk"
      - "makes/shared/**"
      - "Makefile"
      - "tox.ini"
      - "scripts/deptry_scan.py"
      - "scripts/security/**"
      - "scripts/sbom/**"
      - "scripts/release/**"
      - "scripts/bijux-llm-vex/**"
  pull_request:
    paths:
      - ".github/workflows/bijux-llm-vex-ci.yml"
      - "packages/bijux-llm-vex/**"
      - "apis/bijux-llm-vex/**"
      - "configs/bijux-vex/**"
      - "makes/bijux-vex/**"
      - "makes/lint.mk"
      - "makes/test.mk"
      - "makes/api.mk"
      - "makes/quality.mk"
      - "makes/security.mk"
      - "makes/sbom.mk"
      - "makes/publish.mk"
      - "makes/shared/**"
      - "Makefile"
      - "tox.ini"
      - "scripts/deptry_scan.py"
      - "scripts/security/**"
      - "scripts/sbom/**"
      - "scripts/release/**"
      - "scripts/bijux-llm-vex/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bijux-llm-vex-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  ARTIFACTS_DIR: artifacts/bijux-llm-vex
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONUNBUFFERED: "1"

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            packages/bijux-llm-vex/pyproject.toml
            tox.ini

      - name: Run tests
        run: make -C packages/bijux-llm-vex test

      - name: Upload test artifacts
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: bijux-llm-vex-test
          path: ${{ env.ARTIFACTS_DIR }}/test
          if-no-files-found: ignore
          retention-days: 14

  checks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [quality, security, docs, api, sbom]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            packages/bijux-llm-vex/pyproject.toml
            tox.ini

      - name: Run ${{ matrix.target }}
        run: make -C packages/bijux-llm-vex ${{ matrix.target }}

      - name: Upload artifacts
        if: ${{ always() && hashFiles(format('{0}/{1}/**', env.ARTIFACTS_DIR, matrix.target)) != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: bijux-llm-vex-${{ matrix.target }}
          path: ${{ env.ARTIFACTS_DIR }}/${{ matrix.target }}
          if-no-files-found: ignore
          retention-days: 14

  lint:
    runs-on: ubuntu-latest
    needs: [tests, checks]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            packages/bijux-llm-vex/pyproject.toml
            tox.ini

      - name: Run lint
        run: make -C packages/bijux-llm-vex lint

      - name: Upload lint artifacts
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: bijux-llm-vex-lint
          path: ${{ env.ARTIFACTS_DIR }}/lint
          if-no-files-found: ignore
          retention-days: 14
