name: publish-bijux-rar

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/build-release-artifacts.yml
    with:
      package_slug: bijux-rar
      package_dir: packages/compat-bijux-rar
      artifacts_dir: artifacts/compat-bijux-rar
      cache_dependency_path: |
        packages/compat-bijux-rar/pyproject.toml
        pyproject.toml
      build_targets: build

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download publish artifacts
        uses: actions/download-artifact@v5
        with:
          name: bijux-rar-pypi-dist
          path: .github/tmp/bijux-rar-dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: .github/tmp/bijux-rar-dist
          skip-existing: true
