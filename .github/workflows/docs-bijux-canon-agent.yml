name: docs-bijux-canon-agent

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/docs-bijux-canon-agent.yml"
      - "packages/bijux-canon-agent/docs/**"
      - "packages/bijux-canon-agent/CHANGELOG.md"
      - "apis/bijux-canon-agent/**"
      - "scripts/bijux-canon-agent/**"
      - "packages/bijux-canon-agent/mkdocs.yml"
      - "makes/docs.mk"
      - "makes/shared/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: docs-bijux-canon-agent-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: packages/bijux-canon-agent/pyproject.toml

      - name: Install package docs toolchain
        run: make -C packages/bijux-canon-agent install

      - name: Configure Git author
        run: |
          git config user.name "Bijan Mousavi"
          git config user.email "bijan@bijux.io"

      - name: Deploy docs
        run: make -C packages/bijux-canon-agent docs-deploy
