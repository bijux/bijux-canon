name: publish-bijux-canon-reason

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/build-release-artifacts.yml
    with:
      package_slug: bijux-canon-reason
      package_dir: packages/bijux-canon-reason
      artifacts_dir: artifacts/bijux-canon-reason
      cache_dependency_path: |
        packages/bijux-canon-reason/pyproject.toml
        pyproject.toml
      build_targets: "build sbom"
      upload_paths: |
        artifacts/bijux-canon-reason/build/*
        artifacts/bijux-canon-reason/sbom/*

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download publish artifacts
        uses: actions/download-artifact@v5
        with:
          name: bijux-canon-reason-pypi-dist
          path: .github/tmp/bijux-canon-reason-dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: .github/tmp/bijux-canon-reason-dist
          skip-existing: true
