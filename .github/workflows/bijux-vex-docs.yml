name: bijux-vex Docs

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/bijux-vex-docs.yml"
      - "packages/bijux-vex/docs/**"
      - "packages/bijux-vex/README.md"
      - "packages/bijux-vex/CHANGELOG.md"
      - "apis/bijux-vex/**"
      - "scripts/bijux-vex/**"
      - "configs/bijux-vex/mkdocs.yml"
      - "makes/docs.mk"
      - "makes/shared/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bijux-vex-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: packages/bijux-vex/pyproject.toml

      - name: Install package docs toolchain
        run: make -C packages/bijux-vex install

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Deploy docs
        run: make -C packages/bijux-vex docs-deploy
