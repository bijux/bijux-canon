name: publish-bijux-llm-agent

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: publish-bijux-llm-agent
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      ARTIFACTS_DIR: artifacts/bijux-llm-agent

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: packages/bijux-llm-agent/pyproject.toml

      - name: Build distribution and SBOM
        run: |
          make -C packages/bijux-llm-agent build
          make -C packages/bijux-llm-agent sbom

      - name: Stage publish artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .github/tmp/bijux-llm-agent-dist
          cp "${ARTIFACTS_DIR}"/build/*.whl .github/tmp/bijux-llm-agent-dist/
          cp "${ARTIFACTS_DIR}"/build/*.tar.gz .github/tmp/bijux-llm-agent-dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          packages-dir: .github/tmp/bijux-llm-agent-dist
          skip-existing: true

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: bijux-llm-agent-release
          path: |
            ${{ env.ARTIFACTS_DIR }}/build/*
            ${{ env.ARTIFACTS_DIR }}/sbom/*
          if-no-files-found: ignore
          retention-days: 14
