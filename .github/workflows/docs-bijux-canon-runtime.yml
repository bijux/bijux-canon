name: docs-bijux-canon-runtime

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/docs-bijux-canon-runtime.yml"
      - "packages/bijux-canon-runtime/docs/**"
      - "packages/bijux-canon-runtime/CHANGELOG.md"
      - "apis/bijux-canon-runtime/**"
      - "scripts/bijux-canon-runtime/**"
      - "packages/bijux-canon-runtime/mkdocs.yml"
      - "makes/docs.mk"
      - "makes/shared/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: docs-bijux-canon-runtime-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: packages/bijux-canon-runtime/pyproject.toml

      - name: Install package docs toolchain
        run: make -C packages/bijux-canon-runtime install

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Deploy docs
        run: make -C packages/bijux-canon-runtime docs-deploy
