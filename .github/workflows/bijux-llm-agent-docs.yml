name: bijux-agent Docs

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/bijux-agent-docs.yml"
      - "packages/bijux-llm-agent/docs/**"
      - "packages/bijux-llm-agent/CHANGELOG.md"
      - "apis/bijux-llm-agent/**"
      - "scripts/bijux-llm-agent/**"
      - "packages/bijux-llm-agent/mkdocs.yml"
      - "makes/docs.mk"
      - "makes/shared/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bijux-agent-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: packages/bijux-llm-agent/pyproject.toml

      - name: Install package docs toolchain
        run: make -C packages/bijux-llm-agent install

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Deploy docs
        run: make -C packages/bijux-llm-agent docs-deploy
