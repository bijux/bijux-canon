name: bijux-agent Docs

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/bijux-agent-docs.yml"
      - "packages/bijux-agent/docs/**"
      - "packages/bijux-agent/README.md"
      - "packages/bijux-agent/CHANGELOG.md"
      - "scripts/bijux-agent/**"
      - "configs/bijux-agent/mkdocs.yml"
      - "makes/bijux-agent/docs.mk"
      - "makes/shared/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bijux-agent-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: packages/bijux-agent/pyproject.toml

      - name: Install package docs toolchain
        run: make -C packages/bijux-agent install

      - name: Configure Git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Deploy docs
        run: make -C packages/bijux-agent docs-deploy
