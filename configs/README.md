# Monorepo Tooling Configuration

Tooling settings shared by the imported packages live here.

- `configs/<package>/` contains package-specific tooling configuration.
- `configs/shared/` contains shared baselines used across packages.

Runtime-local configuration files that are part of an application interface stay
inside the package directory.
