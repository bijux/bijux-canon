# SSOT

Source-of-truth locations for `bijux-canon-ingest` are:
- `src/bijux_canon_ingest/` for implementation
- `apis/bijux-canon-ingest/v1/` for pinned API schema
- `tests/` for executable ingest and retrieval behavior
- `docs/*.md` here for package-local architecture and scope

Root docs may provide guides, but package-local boundary truth belongs in this directory.
