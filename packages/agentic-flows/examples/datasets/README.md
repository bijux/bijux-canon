This directory contains small, versioned example datasets used to validate
non-determinism behavior and replay policies. These files are tracked with DVC
to keep dataset hashes stable and auditable across examples and test runs.
