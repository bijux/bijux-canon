# SSOT

Source-of-truth locations for `bijux-canon-agent` are:
- `src/bijux_canon_agent/` for implementation
- `tests/` for executable behavior and invariants
- `docs/*.md` in this directory for package-local architecture and scope
- `pyproject.toml` for package metadata and declared public entrypoints

Root documentation may aggregate package information, but package-local ownership truth lives here.
