# TESTS

`bijux-canon-reason` validates behavior through:
- `tests/unit/` for local models and runtime behavior
- `tests/api/` for HTTP contracts
- `tests/e2e/` for package-facing flows
- `tests/regression/` and docs-facing checks where they exist

Changes to reasoning flow, verification semantics, or public interfaces should land with matching tests.
