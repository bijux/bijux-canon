# SCOPE

`bijux-canon-index` exists to execute vector search workloads with explicit provenance and determinism semantics.

Its scope includes:
- backend and plugin-backed vector execution
- artifact comparison and replay-oriented result bookkeeping
- vector execution API behavior

Its scope excludes:
- ingest-specific document normalization
- runtime-wide policy governance
- repository tooling and docs infrastructure
