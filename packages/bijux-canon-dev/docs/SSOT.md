# SSOT

Source-of-truth locations for `bijux-canon-dev` are:
- `src/bijux_canon_dev/` for implementation
- `tests/` for behavior checks
- `docs/*.md` here for package scope and ownership
- root `Makefile`, `makes/`, and CI workflows for live callers of the tooling

If a script-like behavior is still needed by the repo, this package is the first place to look.
