[tox]
minversion = 4.11
requires =
    tox>=4.11
skip_missing_interpreters = true
envlist =
    test-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}
    lint-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}
    quality-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}
    security-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}
    docs-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}
    api-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}
    build-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}
    sbom-{agentic_flows,bijux_canon_agent,bijux_rag,bijux_canon_reason,bijux_canon_index}

[testenv]
package = skip
allowlist_externals = make
change_dir = {env:PACKAGE_DIR}
setenv =
    MAKEFLAGS = VENV={envdir} ACT={envdir}/bin
    PIP_DISABLE_PIP_VERSION_CHECK = 1
    PYTHONDONTWRITEBYTECODE = 1
    agentic_flows: PACKAGE_DIR = {tox_root}/packages/bijux-canon-runtime
    bijux_canon_agent: PACKAGE_DIR = {tox_root}/packages/bijux-canon-agent
    bijux_rag: PACKAGE_DIR = {tox_root}/packages/bijux-canon-ingest
    bijux_canon_reason: PACKAGE_DIR = {tox_root}/packages/bijux-canon-reason
    bijux_canon_index: PACKAGE_DIR = {tox_root}/packages/bijux-canon-index
passenv =
    CI
    GITHUB_*
    GITLAB_*
    PIP_INDEX_URL
    PIP_EXTRA_INDEX_URL
    HTTP_PROXY
    HTTPS_PROXY
    NO_PROXY
    SSH_AUTH_SOCK
    LC_ALL
    LANG
    TC_BASE
    SBOM_CLI
    OPENAPI_GENERATOR_VERSION
    API_HOST
    API_PORT
    API_BASE_PATH
    API_FACTORY
commands_pre =
    security: python -m pip install -U "pip>=25.3"
commands =
    test: make test
    lint: make lint
    quality: make quality
    security: make security
    docs: make docs
    api: make api
    build: make build
    sbom: make sbom
