retry:
  max_attempts: 3
  transient_codes:
    - TIMEOUT
    - TRANSIENT

fallback:
  preferred_models:
    - gpt-4o-mini
    - gpt-4o-mini-0
    - gpt-3.5-turbo-1106

scope_reduction:
  steps:
    - collapse_documents
    - summarize_sources
    - reduce_to_key_points

abort:
  critical_codes:
    - FATAL
    - SECURITY
  max_errors_before_abort: 1
