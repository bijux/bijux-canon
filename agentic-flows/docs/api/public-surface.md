# Public Surface
> Public API surface for v1.

public CLI commands:
run
replay
inspect

public HTTP endpoints:
POST /api/v1/flows/run
POST /api/v1/flows/replay

public schemas:
FlowRunRequest
FlowRunResponse
ReplayRequest
FailureEnvelope

Anything not listed here is internal.
